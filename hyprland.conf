# Display/Monitor set-up
monitor = eDP-1, 1920x1080, 0x0, 1

# Application Variables/Keywords
$terminal = kitty
$files = nemo
$menu = fuzzel
$screenshot = grim -g "$(slurp)" ~/Pictures/Screenshots/$(date +%Y-%m-%d_%H-%M-%S).png
$browser = firefox
$discord = vesktop
$music = spotify
$eyedrop = hyprpicker
$animations = no, please :)
$reader = SDL_VIDEODRIVER=x11 koreader


# Autostart, EnvVars, & Permissions
exec-once = mako # Notification Daemon
exec-once = waybar & hyprpaper # Waybar & Background Wallpaper
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
ecosystem {
  enforce_permissions = 1
}
permission = /usr/(bin|local/bin)/grim, screencopy, allow # allows Grim to take screenshots
permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
permission = /usr/(bin|local/bin)/hyprpm, plugin, allow

# Look and feel
general {
    gaps_in = 4
    gaps_out = 10
    border_size = 1
    # https://wiki.hypr.land/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(6272a4ff) rgba(8be9fdff) rgba(bd93f9ff) rgba(ff79c6ff) 45deg
    col.inactive_border = rgba(2b292eff)
    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false
    allow_tearing = false
    layout = dwindle
}

decoration {
    rounding = 8
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hypr.land/Configuring/Variables/#animations
animations {
    enabled = $animations, please :) # enable for animations

    # Default curves, see https://wiki.hypr.land/Configuring/Animations/#curves
    #        NAME,           X0,   Y0,   X1,   Y1
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1

    # Default animations, see https://wiki.hypr.land/Configuring/Animations/
    #           NAME,          ONOFF, SPEED, CURVE,        [STYLE]
    animation = global,        1,     10,    default
    animation = border,        1,     5.39,  easeOutQuint
    animation = windows,       1,     4.79,  easeOutQuint
    animation = windowsIn,     1,     4.1,   easeOutQuint, popin 87%
    animation = windowsOut,    1,     1.49,  linear,       popin 87%
    animation = fadeIn,        1,     1.73,  almostLinear
    animation = fadeOut,       1,     1.46,  almostLinear
    animation = fade,          1,     3.03,  quick
    animation = layers,        1,     3.81,  easeOutQuint
    animation = layersIn,      1,     4,     easeOutQuint, fade
    animation = layersOut,     1,     1.5,   linear,       fade
    animation = fadeLayersIn,  1,     1.79,  almostLinear
    animation = fadeLayersOut, 1,     1.39,  almostLinear
    animation = workspaces,    1,     1.94,  almostLinear, fade
    animation = workspacesIn,  1,     1.21,  almostLinear, fade
    animation = workspacesOut, 1,     1.94,  almostLinear, fade
    animation = zoomFactor,    1,     7,     quick
}

master {
  new_status = master
}

misc {
  force_default_wallpaper = -1
  disable_hyprland_logo = false
}

# Input
input {
  kb_layout = us
  follow_mouse = 1
  sensitivity = 0.2
  touchpad {
    natural_scroll = true
  }
}
gesture = 3, horizontal, workspace

##########
# Keymaps
$mainMod = SUPER # Sets Super (Windows Key) as main mod key

# Main launchers
bind = $mainMod, Return, exec, $terminal
bind = $mainMod SHIFT, Return, exec, [float; center; size 800 500] $terminal
bind = $mainMod, E, exec, $files
bind = $mainMod, C, exec, $browser
bind = $mainMod, D, exec, $menu
bind = $mainMod, K, exec, $reader
bind = $mainMod SHIFT, Y, exec, $screenshot
bind = $mainMod SHIFT, P, exec, hyprpicker

bind = $mainMod, Q, killactive,
bind = $mainMod, V, togglefloating
# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = ALT, A, movefocus, l
bind = $mainMod, right, movefocus, r
bind = ALT, D, movefocus, r
bind = ALT, W, movefocus, u
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d
bind = ALT, S, movefocus, d
bind = $mainMod, F, fullscreen
# Move Window placement within individual workspace
bind = ALT SHIFT, left, movewindow, l
bind = ALT SHIFT, right, movewindow, r
bind = ALT SHIFT, up, movewindow, u
bind = ALT SHIFT, down, movewindow, d
bind = ALT SHIFT, a, movewindow, l
bind = ALT SHIFT, d, movewindow, r
bind = ALT SHIFT, w, movewindow, u
bind = ALT SHIFT, s, movewindow, d
# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10
# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10
# Special Workspaces
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic
bind = $mainMod, M, togglespecialworkspace, music
bind = $mainMod SHIFT, M, movetoworkspace, special:music
bind = $mainMod, I, togglespecialworkspace, comms
bind = $mainMod SHIFT, I, movetoworkspace, special:comms
bind = $mainMod, G, togglespecialworkspace, gaming
bind = $mainMod SHIFT, G, movetoworkspace, special:gaming
bind = $mainMod, R, togglespecialworkspace, reading
bind = $mainMod SHIFT, R, movetoworkspace, special:reading
# Scroll through existing workspaces with mainMod + Scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1
# Move/resize windows with mainMod + L/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 2%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 2%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 2%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = $mainMod, INSERT, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = $mainMod, END, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = $mainMod, HOME, exec, playerctl previous
bindl = , XF86AudioPrev, exec, playerctl previous

# Window Rules
windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}

